spring:
  application:
    name: hwsmp-audit
  datasource:
    url: jdbc:postgresql://localhost:5432/hwsmp
    username: hwsmp
    password: hwsmp_dev_2024
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_dev_2024

server:
  port: 0
